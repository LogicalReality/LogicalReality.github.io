/**
 * config.js — Configuración centralizada de la aplicación.
 *
 * Un solo lugar para los valores que el resto de la app necesita.
 * Cuando algo cambie (username, colores), se toca acá y solo acá.
 */

export const GITHUB_USERNAME = 'LogicalReality';
export const ACCENT_COLOR = '#00FF41';
